<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <link rel="shortcut icon" type="image/png" href="images/favicon.ico"/>
    <title>USESE</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            height: 100%;
            padding-top: 20px;
            padding-bottom: 20px;
        }

        html, .container-fluid, .row, .col-md-4 {
            height: 100%;
        }

        iframe {
            width: 100%;
            height: 100%;
        }

        /*.thumbnail iframe {
            position: relative;
            z-index: 1;
            width: 1000px;
            height: 750px;

            -webkit-transform-origin: 0 0;
            -moz-transform-origin: 0 0;
            transform-origin: 0 0;


            -webkit-transform:  scale(0.4, 0.4);   /* Gets the size down to fit into your .thumbnail. */
            -moz-transform:  scale(0.4, 0.4);      /* Gets the size down to fit into your .thumbnail. */
            transform:  scale(0.4, 0.4);           /* Gets the size down to fit into your .thumbnail. */
            overflow: hidden;

        }*/
    </style>
</head>
<body>

<script>
    window.onload=onloading;
    function onloading()
    {
        document.getElementById("bing").src="https://www.bing.com/search?q=$query";
        document.getElementById("google").src="https://www.google.com/search?q=$query&igu=1";
    }
</script>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-4">
            <iframe src="/results?query=$query&relevance=1&clustering=1&qe=1" name="ourIframe"></iframe>
        </div>
        <div class="col-md-4">
            <iframe id ="bing"  name="bingIframe"></iframe>
        </div>
        <div class="col-md-4">
            <iframe id="google"  name="googleIframe"></iframe>
        </div>
    </div>
</div>

</body>
</html>
